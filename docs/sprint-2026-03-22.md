# Sprint 交接文档 — 2026-03-22

> 本文档为下一个 Agent 提供上下文，确保无缝衔接。

## 本 Sprint 完成事项

### 1. UI 全面审查修复 (commit `459df2b`)
- 设计系统统一：`DESIGN.md` + `index.html` 主色对齐 `#4b41e1`，分离 warning/danger
- 可访问性：`aria-live` Toast、`role="alert"` 登录错误、语义 `<button>` 替换 `<div onclick>`
- 安全：`showApiKey` XSS 修复（`safeAttr`）、WebSocket token 移出 URL 参数
- 交互：Hash-based URL 路由持久化、`tabular-nums` 指标对齐
- 省略号标准化：`...` → `…`

### 2. 品牌 Logo (commit `2ad9004`)
- 处理用户提供的 S-wave 渐变图标，生成 favicon.ico / 32px / 36px / 72px / 192px / 512px / apple-touch-icon
- 登录页居中大 Logo、侧边栏品牌 Logo
- 登录网络错误区分（TypeError vs 服务端错误）

### 3. TypeScript 全迁移 (commit `90856f7`)
- 16 个 `src/__tests__/*.test.js` → `.test.ts`
- `package.json` test glob 同步更新
- GitHub 语言统计：JavaScript 15% → 0%

### 4. Tailwind v4 本地化 (commits `29debe7` → `696e782`)
- `@tailwindcss/browser` CDN → `@tailwindcss/vite` 本地构建插件
- 创建 `src/client/styles.css`：集中 `@theme` + custom classes + Dark Mode 覆盖
- Google Fonts → `@fontsource-variable/inter|manrope|jetbrains-mono`
- Lucide Icons → `lucide` npm ESM import
- 删除 stale `public/index.html`
- **结果：前端零外部 CDN 请求，GFW 环境完全可用**

### 5. Dark Mode 修复 (commit `fc336af`)
- `.dark` CSS 变量从 `<style type=text/tailwindcss>` 移到独立 `<style>`（后迁入 styles.css）

### 6. README 全面更新 + 部署
- README.md 重写反映当前架构
- 部署到 https://api.synonclaw.com
- 服务端清理旧文件（`git clean -fd` + `vite build`）

### 7. 导航调整 (commit `1cac6a2`)
- 技能商店移到节点管理下方（平级）

## 当前架构状态

```
opengnb-gui/
├── index.html                  # SPA 入口（零 CDN 标签）
├── src/client/
│   ├── styles.css              # 设计系统核心（@theme + Dark Mode）
│   ├── main.ts                 # 入口（import styles.css + lucide）
│   ├── core.ts                 # 状态管理 + Hash 路由
│   ├── utils.ts                # lucide ESM createIcons
│   └── pages/                  # 5 个页面组件
├── src/services/               # 16 个后端服务
├── src/routes/                 # 8 个 API 路由
├── src/middleware/              # 3 个中间件
├── src/__tests__/               # 16 个 .ts 测试文件
├── vite.config.ts              # @tailwindcss/vite 插件
└── scripts/                    # 6 个 shell 脚本
```

**技术栈：** TypeScript 100% | Tailwind v4 (本地构建) | Vite 8 | fontsource 字体 | lucide ESM

## 关键注意事项

### deploy.sh 改进需求
- `git reset --hard` 不清理 untracked 文件，大重构后需手动 `git clean -fd`
- 建议在脚本中加入 `git clean -fd` 步骤

### 构建产物体积
- JS 505KB (gzip 117KB) — 因 `import { icons } from 'lucide'` 导入全量 ~1500 图标
- 可优化：按需导入实际使用的图标（需维护图标列表）

### CSS Lint 警告
- `@theme` at-rule 对 IDE 未知（正常，Tailwind v4 语法）
- `/tmp/process_logo.py` PIL 导入警告（临时脚本，可忽略）

## 部署信息

| 项目 | 值 |
|------|------|
| 服务器 | `root@43.156.128.95` |
| 域名 | `api.synonclaw.com` |
| 应用目录 | `/opt/gnb-console` |
| 服务 | `gnb-console.service` (systemd) |
| 最新 commit | `1cac6a2` |
| Node.js | v24.14.0 |

## 建议的下一步

1. **部署当前 commit** — 导航调整尚未部署到生产（最后只推送了代码，没重新部署）
2. **Lucide 图标 tree-shaking** — 按需导入减小 JS 体积
3. **deploy.sh 加 `git clean -fd`** — 防止重构后旧文件残留
4. **浏览器端视觉验证** — Dark Mode 切换、新 Logo、字体渲染确认
